# Candidate Verification Portal (client)

* Download npm packages

    ```shell
    $ npm install
    ```

* Download bower components

    ```shell
    $ bower install
    ```

* Run the application

    ```shell
    $ gulp
    ```
    
* To run on different Port
    ```shell
    $ export PORT=8080 && gulp
    ```
